<%= simple_form_for(input) do |f| %>
  <div class="form-group">
    <%= f.input :content, label: false, input_html: { placeholder: "Say something...", rows: rows, class: "form-control" } %>
  </div>

  <div class="form-group">
    <%= f.submit submit_text, class: "btn btn-secondary" %>
  </div>
<% end %>
